# Database connection URL (PostgreSQL)
DATABASE_URL="postgresql://postgres:password@localhost:5432/onefi_db?schema=public"

# Backend server port (5001 is recommended on macOS to prevent AirPlay ControlCenter conflicts)
PORT=5001

# Client origin for CORS (Local development)
CLIENT_URL="http://localhost:5173"

# Node environment
NODE_ENV="development"
